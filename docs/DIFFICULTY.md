# Difficulty

Easy through Expert are available. The curated metadata names naked single, hidden single, locked candidate and naked pair profiles. The runtime analyser currently detects naked and hidden singles only, so the higher labels are verified registry classifications rather than a complete in-app regrading pass. Master is feature-hidden because advanced-chain grading is not reliable yet.
