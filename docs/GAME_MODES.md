# Game modes

Classic records normal timed statistics. Quick uses the preferred difficulty. Daily hashes the local date into the verified registry and de-duplicates its completion date. Zen hides pressure and cannot strike out. Time Attack counts down from five minutes and ends at zero. Mistakes are wrong final values confirmed against the local solution; notes, clears, selection and undo never count.
