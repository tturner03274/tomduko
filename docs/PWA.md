# PWA

`vite-plugin-pwa` generates a Workbox service worker and precaches HTML, hashed bundles, fonts, icons and branding. Registration uses prompt mode: an update banner saves current state, updates only on confirmation, reloads and restores. The manifest is standalone, portrait-primary and uses local assets. Safari remains playable before installation.
