import{useState}from'react';export function Onboarding({finish}:{finish:()=>void}){const[step,setStep]=useState(0),pages=[{mark:'crest',title:'Welcome to Tomduko',text:'Your private Sudoku club. No adverts, subscriptions or nonsense.'},{mark:'9',title:'Simple, tactile play',text:'Select a cell, choose a number, and use notes when logic needs room.'},{mark:'◇',title:'Help on your terms',text:'Choose mistake handling, highlights and commentary in Settings.'},{mark:'⌂',title:'Made for your iPhone',text:'In Safari, tap Share then Add to Home Screen. Tomduko works offline after its first visit.'}],p=pages[step];return <div className="onboarding"><button className="skip" onClick={finish}>Skip</button>{p.mark==='crest'?<img className="onboard-logo" src="/branding/tomduko-pwa-source.png" alt="Tomduko crest"/>:<div className="onboard-mark">{p.mark}</div>}<span className="eyebrow">{step+1} OF {pages.length}</span><h1>{p.title}</h1><p>{p.text}</p><button onClick={()=>step===pages.length-1?finish():setStep(step+1)}>{step===pages.length-1?'ENTER TOMDUKO':'CONTINUE'}</button><div className="dots">{pages.map((_,i)=><i key={i} className={i===step?'active':''}/>)}</div></div>}
